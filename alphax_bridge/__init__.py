"""
AlphaX POS Bridge — universal hardware adapter.

Runs as a local daemon on the cashier device (Windows / macOS / Linux / RPi)
and gives the browser-based POS access to receipt printers, cash drawers,
customer displays, and weighing scales.

Hardware-agnostic. Devices are described by JSON/YAML profiles, not code.
"""
__version__ = "15.5.0"
